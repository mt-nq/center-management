spring.application.name=center-management
spring.datasource.url=jdbc:postgresql://localhost:5432/center_management
spring.datasource.username=postgres
spring.datasource.password=phamminhtuan51

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable Flyway migrations (migrations applied manually)
spring.flyway.enabled=false

# Dev credentials for quick API testing (change/remove for production)
spring.security.user.name=admin
spring.security.user.password=admin

spring.profiles.active=dev
